App As appinfo:
    BackEnabled: =false
    OnStart: =ClearCollect(colAttachments,Defaults(Informaition).Attachments)
    Theme: =PowerAppsTheme

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

