App As appinfo:
    BackEnabled: =true
    OnStart: =ClearCollect(colAttachments,Defaults(TBL_CameraPictureFile).Attachments)
    Theme: =PowerAppsTheme

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

